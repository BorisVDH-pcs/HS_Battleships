// Frame-counted from boom-cannon.gif (29 frames at 100ms each) — the moment
// the animation's last frame has shown, not a guess. Shared so the tile
// reveal (useGame.js) and the hit/miss sound (FireEffect.jsx) land at the
// same instant, right after the gif finishes rather than on top of it.
export const GIF_DURATION_MS = 2900;
